var first; 
var second; 
var n; 
var i;
var temp;
 
{n = 10; 
{i=0; 
	while i < n do
		{i = i + 1;
		{if i == 0 
		 then {first = 0; second = 1}
		 else {temp = second; 
			  {second = first + second; 
		      first = temp}}}}}
		}}}}}

var sum; sum = proc n: if 0 < n then {acc = acc + n; {n = n-1; sum(n)}} else skip

var sum; 
var acc;
{acc = 0; 
	{
		{sum = proc n: if 0 < n 
		   	   then {acc = acc + n;
		   	   			{n = n-1; 
		   	   			sum(n)}
		   	   		} 
		   else skip
		}; 
		sum(10)}}"

var sum; var acc; {acc = 0; {{sum = proc n: if 0 < n then {acc = acc + n; {n = n-1; sum(n)}} else skip}; sum(10)}}"